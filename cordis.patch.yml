# The ui-status-label bundle patch: mounts the plugin row into the Web roster.
# Web profiles (dsh-web-app) already list this row in-box; this patch exists so
# older or custom compositions can install the plugin standalone.
- insert:
    - id: ui-status-label
      name: '@deepseek-ai/dsh-client-ui-status-label'
